# 更新日誌

本檔案記錄此 NanoClaw Fork 的所有重要變更。

格式基於 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.0.0/)，
本專案遵循[語意化版本](https://semver.org/lang/zh-TW/)。

<p align="right">
  <a href="../../CHANGELOG.md">English</a> | <b>繁體中文</b>
</p>

## [2.0.0] - 2024-02-07

### 🎉 重大發布：進階 Skills 架構

此版本對 Skills 系統進行了全面改造，使 NanoClaw 更加模組化、安全且開發者友善。

### 新增功能

#### 核心功能
- **共享 Skills 目錄**（`/skills/`）- 所有群組現在都能存取通用 skills
- **聲明式依賴管理**（`deps.json`）- 每個 skill 聲明自己的依賴
- **多階段 Docker 構建**（`Dockerfile.skills`）- 高效的層快取和條件式構建
- **智能構建腳本** - 自動偵測啟用的 skills 及其依賴
- **開發模式**（`dev.sh`）- 即時 skill 掛載，快速開發
- **套件驗證** - 安全檢查以防止依賴注入攻擊

#### 新檔案
- `skills/README.md` - 完整的 skills 文件
- `skills/*/deps.json` - 每個 skill 的依賴聲明
- `container/Dockerfile.skills` - 多階段構建配置
- `container/dev.sh` - 開發模式助手腳本
- `container/docker-compose.dev.yml` - 開發環境設定

### 變更內容

#### 架構改進
- **Skills 位置**：從 `.claude/skills/` 移至 `/skills/`（頂層目錄）
- **容器掛載**：為所有群組新增 `/workspace/shared-skills` 掛載點
- **構建流程**：從單體到多階段，支援條件式依賴
- **依賴安裝**：從寫死到透過 `deps.json` 聲明

#### 增強檔案
- `container/build.sh` - 新增 skill 偵測、驗證和條件式構建
- `src/container-runner.ts` - 為所有容器新增共享 skills 掛載
- `README.md` - 完整改寫，包含詳細文件

### 安全性

- **套件名稱驗證**：防止依賴名稱中的命令注入
- **唯讀掛載**：共享 skills 以唯讀方式掛載到容器
- **群組隔離**：每個群組維護獨立的可寫入 skill 目錄

### 效能

- **Docker 層快取**：多階段構建減少重建時間
- **條件式依賴**：只安裝需要的內容（更小的映像檔）
- **共享映像檔**：多個機器人可使用相同容器映像檔

### 開發者體驗

- **熱重載**：開發模式允許無需重建即可測試
- **清晰文件**：新增和管理 skills 的完整指南
- **向後相容**：`--original` 標誌可使用原始 Dockerfile

## [1.0.0] - 原始版本

### 初始 Fork 功能（來自 gavrielc/nanoclaw）

- 透過 Baileys 整合 WhatsApp
- Docker 容器隔離
- 每群組記憶（CLAUDE.md）
- 任務排程（cron、間隔、一次性）
- `.claude/skills/` 中的基本 skills
- Apple Container 支援（macOS）

---

## 遷移指南

### 從原始版本到 2.0.0

1. **更新倉庫**
   ```bash
   git pull origin main
   ```

2. **移動自訂 Skills**
   ```bash
   # 如果你在 .claude/skills/ 有自訂 skills
   mv .claude/skills/my-skill skills/
   ```

3. **為 Skills 新增 deps.json**
   為每個 skill 建立 `deps.json`（見 `/skills/` 中的範例）

4. **重建容器**
   ```bash
   cd container
   ./build.sh
   ```

5. **測試**
   ```bash
   npm run dev
   # 或用於生產環境
   docker compose -f docker-compose.vps.yml up
   ```

### 破壞性變更

- Skills 現在必須有 `deps.json` 檔案
- `.claude/skills/` 目錄已棄用（使用 `/skills/`）
- 容器映像檔名稱改為 `nanoclaw-agent`（原為 `nanoclaw`）

### 棄用項目

- `.claude/skills/` 目錄（已移至 `/skills/`）
- Dockerfile 中的寫死依賴
- 單階段 Docker 構建

---

## 發展藍圖

### 下一個版本規劃

- [ ] Skills 市集/註冊表
- [ ] 執行時依賴安裝
- [ ] Skills 版本控制和更新
- [ ] Skill 管理的 Web UI
- [ ] 自動化 skill 測試框架
- [ ] Fork 間的 Skills 分享

### 考慮中

- Skill 管理的 GraphQL API
- 以 Rust/Go 編寫的 Skills
- Skill 載入器的外掛系統
- 雲端 skill 儲存

---

## 貢獻者

- 原作者：[@gavrielc](https://github.com/gavrielc)
- Skills 架構：本 Fork 貢獻者

---

## 支援

相關問題：
- **原始功能**：[gavrielc/nanoclaw/issues](https://github.com/gavrielc/nanoclaw/issues)
- **Skills 系統**：[本 Fork 的 issues](https://github.com/aceonaceon/nanoclaw/issues)